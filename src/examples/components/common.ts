export type TProps = {
  isMemoized: boolean;
  changeProps: boolean;
};

export type TValue = {
  value: number;
};

export type TObjectValue = {
  value: {
    num: number;
    str: string;
  };
};
