export type TProps = {
  isMemoized: boolean;
  changeProps: boolean;
};

export type TValue = {
  value: number;
};

export type TObjectProps = {
  obj: TObjectValue;
};

export type TObjectValue = {
  num: number;
  str: string;
};
